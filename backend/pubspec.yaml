name: kupalo
description: A server app using the shelf package and Docker.
version: 1.0.0
repository: https://github.com/zzundalek/kupalo

environment:
  sdk: ^3.10.0

dependencies:
  postgres: ^3.5.12
  shelf: ^1.4.2
  shelf_router: ^1.1.2

dev_dependencies:
  http: ^1.2.2
  lints: ^6.0.0
  test: ^1.25.6
  very_good_analysis: ^10.1.0
